export enum Status {
    todo = 'todo',
    inPogress = 'in-progress',
    done = 'done'
}

export enum CommandName {
    list = 'list',
    add = 'add',
    update = 'update',
    mark = 'mark',
    delete = 'delete',
    help = 'help'
}

export const UNDEFINED_ID = -1;
