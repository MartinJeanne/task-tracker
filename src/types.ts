export enum Status {
    todo = 'todo',
    inPogress = 'in-progress',
    done = 'done'
}

export enum CommandName {
    list = 'list',
    add = 'add',
    update = 'update',
    mark = 'mark',
    delete = 'delete'
}

export const UNDEFINED_ID = -1;
